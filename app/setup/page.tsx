export default function SetupPage() {
  return <main className="min-h-screen grid place-items-center p-6"><section className="panel max-w-xl rounded-3xl p-8"><div className="mb-6 inline-flex rounded-full border border-amber-400/20 bg-amber-400/10 px-3 py-1 text-xs text-amber-200">Configuration requise</div><h1 className="text-3xl font-semibold tracking-tight">Sales Intelligence</h1><p className="mt-3 text-sm leading-6 text-white/55">L’application est déployable, mais aucune donnée commerciale n’est accessible tant qu’un projet Supabase dédié et ses variables publiques ne sont pas configurés côté serveur.</p><div className="mt-6 rounded-2xl border border-white/8 bg-black/20 p-4 text-sm text-white/65"><p>Nécessaire : <code>NEXT_PUBLIC_SUPABASE_URL</code> et <code>NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY</code>.</p><p className="mt-2">Les clés privées, conversations et numéros WhatsApp ne doivent jamais être placés dans GitHub.</p></div></section></main>;
}
