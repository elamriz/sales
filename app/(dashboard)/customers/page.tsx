import { createClient } from "@/lib/supabase/server"; import { eur } from "@/lib/dashboard";
export default async function Customers(){const s=await createClient();const {data}=await s.from("customers").select("id,display_name,first_seen_at,last_seen_at,first_order_at,last_order_at,sales_episodes(id,total,refund_amount,classification,revenue_eligible,ordered_at)").order("last_seen_at",{ascending:false}).limit(200);const rows=(data||[]).map((c:any)=>{const orders=(c.sales_episodes||[]).filter((o:any)=>o.classification==="confirmed"&&o.revenue_eligible&&o.total!=null);const ca=orders.reduce((n:number,o:any)=>n+Math.max(Number(o.total)-Number(o.refund_amount||0),0),0);return {...c,orders:orders.length,ca,aov:orders.length?ca/orders.length:0,status:orders.length>=5?"VIP":orders.length>1?"Récurrent":"Nouveau"};});return <div><div className="mb-6"><p className="text-xs uppercase tracking-[.18em] text-violet-300">Customers</p><h1 className="mt-1 text-3xl font-semibold">Clients</h1></div><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{rows.map((c:any)=><article key={c.id} className="panel rounded-2xl p-5"><div className="flex items-start justify-between"><div><h2 className="font-medium">{c.display_name||"Client"}</h2><p className="mt-1 text-xs text-white/35">{c.status}</p></div><span className="rounded-full bg-white/5 px-2 py-1 text-xs text-white/45">{c.orders} commandes</span></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><p className="text-xs text-white/35">LTV confirmée</p><p className="mt-1 font-medium">{eur(c.ca)}</p></div><div><p className="text-xs text-white/35">Panier moyen</p><p className="mt-1 font-medium">{eur(c.aov)}</p></div><div><p className="text-xs text-white/35">Première commande</p><p className="mt-1 text-white/60">{c.first_order_at?new Date(c.first_order_at).toLocaleDateString("fr-BE"):"—"}</p></div><div><p className="text-xs text-white/35">Dernière commande</p><p className="mt-1 text-white/60">{c.last_order_at?new Date(c.last_order_at).toLocaleDateString("fr-BE"):"—"}</p></div></div></article>)}{!rows.length&&<div className="panel rounded-2xl p-8 text-sm text-white/35">Aucun client structuré dans la base dédiée.</div>}</div></div>}
