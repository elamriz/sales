import { RevenueChart } from "@/components/revenue-chart"; import { StatCard } from "@/components/stat-card"; import { eur,loadOverview } from "@/lib/dashboard"; import { createClient } from "@/lib/supabase/server";
export default async function Analytics(){const x=await loadOverview("all");const s=await createClient();const {data:orders}=await s.from("sales_episodes").select("customer_id,ordered_at,total,refund_amount,classification,revenue_eligible").eq("classification","confirmed").eq("revenue_eligible",true).order("ordered_at");const byCustomer=new Map<string,Date[]>();for(const o of (orders||[]) as any[]){if(!o.customer_id||!o.ordered_at)continue;const a=byCustomer.get(o.customer_id)||[];a.push(new Date(o.ordered_at));byCustomer.set(o.customer_id,a);}const delays:number[]=[];for(const a of byCustomer.values()){a.sort((a,b)=>+a-+b);if(a.length>1)delays.push((+a[1]-+a[0])/86400000);}const avgDelay=delays.length?delays.reduce((a,b)=>a+b,0)/delays.length:0;const distribution=new Map<number,number>();for(const a of byCustomer.values())distribution.set(a.length,(distribution.get(a.length)||0)+1);return <div className="space-y-6"><div><p className="text-xs uppercase tracking-[.18em] text-violet-300">Analytics</p><h1 className="mt-1 text-3xl font-semibold">Analyse commerciale</h1></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="CA historique confirmé" value={eur(x.confirmed)}/><StatCard label="Panier moyen" value={eur(x.aov)}/><StatCard label="Taux de réachat" value={`${(x.repurchaseRate*100).toFixed(1)} %`}/><StatCard label="Délai moyen avant 2e commande" value={delays.length?`${avgDelay.toFixed(1)} jours`:"—"}/></div><section className="panel rounded-2xl p-5"><h2 className="font-medium">CA par jour</h2><p className="mt-1 text-xs text-white/35">Le potentiel reste visuellement et comptablement séparé.</p><div className="mt-4"><RevenueChart data={x.chart}/></div></section><section className="panel rounded-2xl p-5"><h2 className="font-medium">Distribution des commandes par client</h2><div className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{[...distribution.entries()].sort((a,b)=>a[0]-b[0]).map(([count,customers])=><div key={count} className="rounded-xl border border-white/7 bg-white/[.02] p-4"><p className="text-2xl font-semibold">{customers}</p><p className="mt-1 text-xs text-white/40">clients avec {count} commande{count>1?"s":""}</p></div>)}</div></section><section className="panel rounded-2xl p-5"><h2 className="font-medium">Taux de conversion</h2><p className="mt-2 text-sm leading-6 text-white/45">Non publié tant que tous les chats n’ont pas reçu un <code>chat_kind</code> fiable. Les broadcasts, tests et conversations non commerciales doivent être exclus avant de produire ce ratio.</p></section></div>}
