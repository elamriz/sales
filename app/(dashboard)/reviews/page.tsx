import { loadReviews, eur } from "@/lib/dashboard"; import { approve,reject } from "./actions";
export default async function Reviews(){const rows:any[]=await loadReviews();return <div><div className="mb-6"><p className="text-xs uppercase tracking-[.18em] text-amber-300">Reviews</p><h1 className="mt-1 text-3xl font-semibold">À valider</h1><p className="mt-2 text-sm text-white/40">Rien ici n’entre dans le CA confirmé tant que la validation n’est pas faite.</p></div><div className="grid gap-3">{rows.map(r=><article key={r.id} className="panel rounded-2xl p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="font-medium">{r.customers?.display_name||"Client"}</p><p className="mt-1 text-xs text-white/35">{r.ordered_at?new Date(r.ordered_at).toLocaleString("fr-BE"):"Date incertaine"} · confiance {Math.round(Number(r.confidence)*100)}%</p></div><p className="text-lg font-semibold">{r.total==null?"Montant incertain":eur(Number(r.total))}</p></div><p className="mt-4 text-sm leading-6 text-white/55">{r.analysis_summary}</p><div className="mt-4 rounded-xl bg-black/20 p-3 text-xs text-white/40">{JSON.stringify(r.uncertainty||{})}</div><div className="mt-4 flex gap-2"><form action={approve}><input type="hidden" name="id" value={r.id}/><button className="rounded-lg bg-emerald-400 px-3 py-2 text-xs font-semibold text-black">Valider</button></form><form action={reject}><input type="hidden" name="id" value={r.id}/><button className="rounded-lg border border-white/10 px-3 py-2 text-xs text-white/60">Rejeter</button></form></div></article>)}{!rows.length&&<div className="panel rounded-2xl p-8 text-center text-sm text-white/35">Aucune validation en attente.</div>}</div></div>}
