import { createClient } from "@/lib/supabase/server";
export default async function Stock(){const s=await createClient();const {data}=await s.from("stock_requests").select("id,product_name,requested_at,quantity,status,confidence,customers(display_name)").order("requested_at",{ascending:false}).limit(200);const rows=data||[];return <div><div className="mb-6"><p className="text-xs uppercase tracking-[.18em] text-violet-300">Stock</p><h1 className="mt-1 text-3xl font-semibold">Demandes de réassort</h1></div><div className="panel overflow-x-auto rounded-2xl"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b border-white/8 text-xs text-white/35"><tr><th className="px-4 py-3">Client</th><th className="px-4 py-3">Produit</th><th className="px-4 py-3">Quantité</th><th className="px-4 py-3">Date</th><th className="px-4 py-3">Confiance</th><th className="px-4 py-3">Statut</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id} className="border-b border-white/5"><td className="px-4 py-3">{r.customers?.display_name||"Client"}</td><td className="px-4 py-3">{r.product_name}</td><td className="px-4 py-3">{r.quantity||"—"}</td><td className="px-4 py-3 text-white/50">{new Date(r.requested_at).toLocaleDateString("fr-BE")}</td><td className="px-4 py-3">{Math.round(Number(r.confidence)*100)}%</td><td className="px-4 py-3 text-white/55">{r.status}</td></tr>)}</tbody></table>{!rows.length&&<p className="p-8 text-center text-sm text-white/35">Aucune demande de stock ouverte.</p>}</div></div>}
